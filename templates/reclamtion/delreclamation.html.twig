{% extends 'base.html.twig' %}
{% block content %}
{% endblock %}

{% block body %}

    <div class="tab-content" id="reclamationtab">
        <div class="tab-pane fade active show" id="reclamationdetail" role="tabpanel" aria-labelledby="reclamation-detail-tab">
            <section class="space-ptb">
                <div class="container">
                    {{ form_start(form) }}
                    {{ form_row(form.description_rec,{ 'attr':{'placeholder':"reclamation decription" ,'class':"form-control"}}) }}
                    {{ form_row(form.Add,{ 'attr':{ 'class':"btn btn-primary"}}) }}
                    {{ form_end(form) }}
                </div>
            </section>
        </div>
    </div>
{% endblock %}