{% extends 'base.html.twig' %}
{% block content %}
{% endblock %}

{% block body %}

    <div class="tab-content" id="formationtab">
        <div class="tab-pane fade active show" id="formation-detail" role="tabpanel" aria-labelledby="formation-detail-tab">
            <section class="space-ptb">
                <div class="container">
                    {{ form_start(form) }}
                    {{ form_row(form.nom,{ 'attr':{'placeholder':" Title" , 'class':"form-control"}}) }}
                    {{ form_row(form.mail,{ 'attr':{'placeholder':"mail" ,'class':"form-control"}}) }}
                    {{ form_row(form.Add,{ 'attr':{ 'class':"btn btn-primary"}}) }}
                    {{ form_end(form) }}
                </div>
            </section>
        </div>
    </div>







{% endblock %}




