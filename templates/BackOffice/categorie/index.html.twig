{% extends 'baseAdmin.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie index</h1>
    {% for message in app.flashes('info') %}
    <strong>Félicitation !</strong>
        <div class="alert-message">
            {{ message }}
        </div>
{% endfor %}
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CategorieName</th>
                <th>CategorieDesc</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.id }}</td>
                <td>{{ categorie.categorieName }}</td>
                <td>{{ categorie.categorieDesc }}</td>
                <td>
                    <a href="{{ path('categorie_show_back', {'id': categorie.id}) }}">show</a>
                    <a href="{{ path('categorie_edit_back', {'id': categorie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_new_back') }}">Create new</a>
{% endblock %}
