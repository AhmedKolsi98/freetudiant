{% extends 'baseAdmin.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>CategorieName</th>
                <td>{{ categorie.categorieName }}</td>
            </tr>
            <tr>
                <th>CategorieDesc</th>
                <td>{{ categorie.categorieDesc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index_back') }}">back to list</a>

    <a href="{{ path('categorie_edit_back', {'id': categorie.id}) }}">edit</a>

    {{ include('BackOffice/categorie/_delete_form.html.twig') }}
{% endblock %}
